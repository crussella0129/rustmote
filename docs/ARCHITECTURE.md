# Architecture

Placeholder. Written in Phase 15 (TASK-015) per `RUSTMOTE_SPEC.md` §8.

Will cover: data flow through `rustmote-core` → `rustmote-cli`, the three credential modes, the SSH-tunnel-to-relay model, and the relay lifecycle state machine — with one mermaid diagram per major flow.
