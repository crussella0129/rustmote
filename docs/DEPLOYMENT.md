# Deployment

Placeholder. Written in Phase 15 (TASK-015) per `RUSTMOTE_SPEC.md` §8.

Will cover a ZimaBoard relay setup walkthrough with exact commands: `server add` → `relay bootstrap` → first successful `connect`.
