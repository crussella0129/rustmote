# Rustmote Relay — Docker Compose

Placeholder. The shipped `docker-compose.yml` template lands in Phase 14 (TASK-014) per `RUSTMOTE_SPEC.md` §5.0.

Once shipped, ports bind to `127.0.0.1` only and the relay is reached exclusively through the SSH tunnel Rustmote establishes. Do not deploy this compose file on a server without a firewall blocking those ports from the LAN.
